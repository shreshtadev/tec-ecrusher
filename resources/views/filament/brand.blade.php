<div class="flex items-center gap-2">
    <img src="{{ asset('images/logos/SSC_LOGO.svg') }}" alt="Logo" class="h-16 w-auto">
    <span class="font-bold text-lg">
        {{ config('app.name') }}
    </span>

</div>
